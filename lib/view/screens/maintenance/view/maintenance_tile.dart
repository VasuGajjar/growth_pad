import 'package:flutter/material.dart';

class MaintenanceTile extends StatelessWidget {
  const MaintenanceTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
